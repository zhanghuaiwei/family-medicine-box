# 家庭药箱管理系统 - 数据库设计文档

## 1. 数据库设计概述

家庭药箱管理系统的数据库设计采用MySQL 8+作为存储引擎，使用UUID作为主键，实现了逻辑删除功能。数据库设计遵循规范化原则，确保数据的一致性、完整性和可扩展性。

## 2. 数据库架构

### 2.1 数据库选择

- **数据库系统**: MySQL 8+
- **字符集**: utf8mb4
- **校对规则**: utf8mb4_unicode_ci
- **存储引擎**: InnoDB

### 2.2 数据库创建

```sql
CREATE DATABASE IF NOT EXISTS medicine_box DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

## 3. 表结构设计

### 3.1 核心表结构

#### 3.1.1 用户表 (users)

| 字段名 | 数据类型 | 约束 | 描述 |
|--------|----------|------|------|
| id | VARCHAR(36) | PRIMARY KEY | 用户ID (UUID) |
| username | VARCHAR(50) | NOT NULL | 用户名 |
| password | VARCHAR(100) | NOT NULL | 密码 (加密存储) |
| phone | VARCHAR(20) | NOT NULL UNIQUE | 手机号 |
| email | VARCHAR(100) | UNIQUE | 邮箱 |
| nickname | VARCHAR(50) | | 昵称 |
| avatar | VARCHAR(255) | | 头像URL |
| gender | TINYINT | | 性别 (0-未知, 1-男, 2-女) |
| birthdate | DATE | | 出生日期 |
| role | TINYINT | NOT NULL DEFAULT 0 | 角色 (0-普通用户, 1-管理员) |
| status | TINYINT | NOT NULL DEFAULT 1 | 状态 (1-正常, 0-禁用) |
| last_login_time | DATETIME | | 最后登录时间 |
| create_time | DATETIME | NOT NULL DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| update_time | DATETIME | NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 更新时间 |
| deleted | TINYINT | NOT NULL DEFAULT 0 | 逻辑删除 (0-未删除, 1-已删除) |

#### 3.1.2 药品分类表 (medicine_categories)

| 字段名 | 数据类型 | 约束 | 描述 |
|--------|----------|------|------|
| id | VARCHAR(36) | PRIMARY KEY | 分类ID (UUID) |
| category_name | VARCHAR(50) | NOT NULL | 分类名称 |
| parent_id | VARCHAR(36) | | 父分类ID |
| description | VARCHAR(255) | | 分类描述 |
| create_time | DATETIME | NOT NULL DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| deleted | TINYINT | NOT NULL DEFAULT 0 | 逻辑删除 (0-未删除, 1-已删除) |
| FOREIGN KEY (parent_id) REFERENCES medicine_categories(id) | | | 外键关联 |

#### 3.1.3 药品表 (medicines)

| 字段名 | 数据类型 | 约束 | 描述 |
|--------|----------|------|------|
| id | VARCHAR(36) | PRIMARY KEY | 药品ID (UUID) |
| name | VARCHAR(100) | NOT NULL | 药品名称 |
| category_id | VARCHAR(36) | | 分类ID |
| brand | VARCHAR(100) | | 品牌 |
| specification | VARCHAR(100) | NOT NULL | 规格 |
| dosage_form | VARCHAR(50) | | 剂型 (片剂、胶囊、颗粒等) |
| usage | TEXT | | 用法用量 |
| indications | TEXT | | 适应症 |
| contraindications | TEXT | | 禁忌症 |
| side_effects | TEXT | | 不良反应 |
| storage_condition | VARCHAR(255) | | 储存条件 |
| expiry_date_days | INT | | 保质期 (天数) |
| manufacturer | VARCHAR(100) | | 生产厂家 |
| approval_number | VARCHAR(50) | | 批准文号 |
| price | DECIMAL(10,2) | | 价格 |
| barcode | VARCHAR(50) | UNIQUE | 条形码 |
| status | TINYINT | NOT NULL DEFAULT 1 | 状态 (1-正常, 0-停用) |
| create_time | DATETIME | NOT NULL DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| update_time | DATETIME | NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 更新时间 |
| deleted | TINYINT | NOT NULL DEFAULT 0 | 逻辑删除 (0-未删除, 1-已删除) |
| FOREIGN KEY (category_id) REFERENCES medicine_categories(id) | | | 外键关联 |

#### 3.1.4 库存表 (inventory)

| 字段名 | 数据类型 | 约束 | 描述 |
|--------|----------|------|------|
| id | VARCHAR(36) | PRIMARY KEY | 库存ID (UUID) |
| user_id | VARCHAR(36) | NOT NULL | 用户ID |
| medicine_id | VARCHAR(36) | NOT NULL | 药品ID |
| batch_number | VARCHAR(50) | | 批次号 |
| purchase_date | DATE | | 购买日期 |
| expiry_date | DATE | NOT NULL | 过期日期 |
| current_quantity | INT | NOT NULL | 当前数量 |
| min_stock | INT | NOT NULL DEFAULT 1 | 最低库存 |
| unit | VARCHAR(20) | NOT NULL | 单位 (瓶、盒、片等) |
| location | VARCHAR(100) | | 存放位置 |
| last_update_time | DATETIME | NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 最后更新时间 |
| deleted | TINYINT | NOT NULL DEFAULT 0 | 逻辑删除 (0-未删除, 1-已删除) |
| FOREIGN KEY (user_id) REFERENCES users(id) | | | 外键关联 |
| FOREIGN KEY (medicine_id) REFERENCES medicines(id) | | | 外键关联 |

#### 3.1.5 库存变动记录表 (inventory_history)

| 字段名 | 数据类型 | 约束 | 描述 |
|--------|----------|------|------|
| id | VARCHAR(36) | PRIMARY KEY | 记录ID (UUID) |
| user_id | VARCHAR(36) | NOT NULL | 用户ID |
| inventory_id | VARCHAR(36) | NOT NULL | 库存ID |
| medicine_id | VARCHAR(36) | NOT NULL | 药品ID |
| change_type | TINYINT | NOT NULL | 变动类型 (1-增加, 2-减少) |
| change_quantity | INT | NOT NULL | 变动数量 |
| before_quantity | INT | NOT NULL | 变动前数量 |
| after_quantity | INT | NOT NULL | 变动后数量 |
| reason | VARCHAR(255) | | 变动原因 |
| change_time | DATETIME | NOT NULL DEFAULT CURRENT_TIMESTAMP | 变动时间 |
| FOREIGN KEY (user_id) REFERENCES users(id) | | | 外键关联 |
| FOREIGN KEY (inventory_id) REFERENCES inventory(id) | | | 外键关联 |
| FOREIGN KEY (medicine_id) REFERENCES medicines(id) | | | 外键关联 |

#### 3.1.6 用药记录表 (usage_records)

| 字段名 | 数据类型 | 约束 | 描述 |
|--------|----------|------|------|
| id | VARCHAR(36) | PRIMARY KEY | 记录ID (UUID) |
| user_id | VARCHAR(36) | NOT NULL | 用户ID |
| medicine_id | VARCHAR(36) | NOT NULL | 药品ID |
| usage_quantity | INT | NOT NULL | 使用数量 |
| usage_time | DATETIME | NOT NULL | 使用时间 |
| notes | VARCHAR(255) | | 备注 |
| create_time | DATETIME | NOT NULL DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| FOREIGN KEY (user_id) REFERENCES users(id) | | | 外键关联 |
| FOREIGN KEY (medicine_id) REFERENCES medicines(id) | | | 外键关联 |

#### 3.1.7 报警表 (alerts)

| 字段名 | 数据类型 | 约束 | 描述 |
|--------|----------|------|------|
| id | VARCHAR(36) | PRIMARY KEY | 报警ID (UUID) |
| user_id | VARCHAR(36) | NOT NULL | 用户ID |
| medicine_id | VARCHAR(36) | | 药品ID |
| alert_type | TINYINT | NOT NULL | 报警类型 (1-低库存, 2-过期, 3-即将过期) |
| alert_level | TINYINT | NOT NULL | 报警级别 (1-低, 2-中, 3-高) |
| message | VARCHAR(255) | NOT NULL | 报警消息 |
| status | TINYINT | NOT NULL DEFAULT 0 | 状态 (0-未处理, 1-已处理) |
| send_time | DATETIME | | 发送时间 |
| resolve_time | DATETIME | | 解决时间 |
| create_time | DATETIME | NOT NULL DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| FOREIGN KEY (user_id) REFERENCES users(id) | | | 外键关联 |
| FOREIGN KEY (medicine_id) REFERENCES medicines(id) | | | 外键关联 |

#### 3.1.8 报警设置表 (alert_settings)

| 字段名 | 数据类型 | 约束 | 描述 |
|--------|----------|------|------|
| id | VARCHAR(36) | PRIMARY KEY | 设置ID (UUID) |
| user_id | VARCHAR(36) | NOT NULL | 用户ID |
| alert_type | TINYINT | NOT NULL | 报警类型 (1-低库存, 2-过期, 3-即将过期) |
| alert_level | TINYINT | NOT NULL | 报警级别 (1-低, 2-中, 3-高) |
| threshold | INT | | 阈值 (如低库存阈值) |
| enabled | TINYINT | NOT NULL DEFAULT 1 | 是否启用 (1-启用, 0-禁用) |
| notification_methods | VARCHAR(50) | | 通知方式 (如 "sms,wechat,email") |
| update_time | DATETIME | NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 更新时间 |
| deleted | TINYINT | NOT NULL DEFAULT 0 | 逻辑删除 (0-未删除, 1-已删除) |
| FOREIGN KEY (user_id) REFERENCES users(id) | | | 外键关联 |

#### 3.1.9 药品推荐表 (medicine_recommendations)

| 字段名 | 数据类型 | 约束 | 描述 |
|--------|----------|------|------|
| id | VARCHAR(36) | PRIMARY KEY | 推荐ID (UUID) |
| category | VARCHAR(50) | NOT NULL | 分类 (如 "感冒药", "退烧药") |
| target_group | VARCHAR(50) | | 目标人群 (如 "儿童", "老人") |
| season | VARCHAR(20) | | 季节 (如 "春季", "冬季") |
| recommended_medicines | TEXT | | 推荐药品 (JSON格式) |
| description | VARCHAR(255) | | 描述 |
| create_time | DATETIME | NOT NULL DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| update_time | DATETIME | NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 更新时间 |
| deleted | TINYINT | NOT NULL DEFAULT 0 | 逻辑删除 (0-未删除, 1-已删除) |

#### 3.1.10 通知记录表 (notifications)

| 字段名 | 数据类型 | 约束 | 描述 |
|--------|----------|------|------|
| id | VARCHAR(36) | PRIMARY KEY | 通知ID (UUID) |
| user_id | VARCHAR(36) | NOT NULL | 用户ID |
| type | TINYINT | NOT NULL | 通知类型 (1-系统, 2-报警, 3-提醒) |
| title | VARCHAR(100) | NOT NULL | 通知标题 |
| content | TEXT | NOT NULL | 通知内容 |
| is_read | TINYINT | NOT NULL DEFAULT 0 | 是否已读 (0-未读, 1-已读) |
| send_time | DATETIME | NOT NULL DEFAULT CURRENT_TIMESTAMP | 发送时间 |
| read_time | DATETIME | | 阅读时间 |
| FOREIGN KEY (user_id) REFERENCES users(id) | | | 外键关联 |

## 4. 索引设计

### 4.1 主键索引

- 所有表的 `id` 字段都设置为主键，使用 UUID 格式

### 4.2 普通索引

| 表名 | 索引名 | 索引字段 | 用途 |
|------|--------|----------|------|
| users | idx_users_phone | phone | 加速手机号查询 |
| users | idx_users_email | email | 加速邮箱查询 |
| medicines | idx_medicines_category_id | category_id | 加速按分类查询药品 |
| medicines | idx_medicines_barcode | barcode | 加速按条形码查询药品 |
| inventory | idx_inventory_user_id | user_id | 加速查询用户库存 |
| inventory | idx_inventory_medicine_id | medicine_id | 加速查询药品库存 |
| inventory | idx_inventory_expiry_date | expiry_date | 加速查询过期药品 |
| inventory_history | idx_inventory_history_inventory_id | inventory_id | 加速查询库存变动记录 |
| usage_records | idx_usage_records_user_id | user_id | 加速查询用户用药记录 |
| usage_records | idx_usage_records_medicine_id | medicine_id | 加速查询药品使用记录 |
| alerts | idx_alerts_user_id | user_id | 加速查询用户报警 |
| alerts | idx_alerts_medicine_id | medicine_id | 加速查询药品报警 |
| alerts | idx_alerts_status | status | 加速查询未处理报警 |
| alert_settings | idx_alert_settings_user_id | user_id | 加速查询用户报警设置 |
| notifications | idx_notifications_user_id | user_id | 加速查询用户通知 |
| notifications | idx_notifications_is_read | is_read | 加速查询未读通知 |

## 5. 数据完整性设计

### 5.1 主键约束

- 所有表都使用 UUID 作为主键，确保全局唯一性
- 主键字段不允许为空

### 5.2 外键约束

- 建立了合理的外键关系，确保数据一致性
- 外键引用的是相关表的主键

### 5.3 非空约束

- 对必要字段设置了非空约束，确保数据完整性
- 如用户名、密码、手机号等

### 5.4 唯一约束

- 对需要唯一的字段设置了唯一约束
- 如手机号、邮箱、条形码等

### 5.5 默认值

- 对部分字段设置了默认值，提高数据录入效率
- 如状态字段默认为1，逻辑删除字段默认为0

## 6. 逻辑删除设计

- 采用逻辑删除机制，通过 `deleted` 字段标记数据是否删除
- `deleted` 字段为 TINYINT 类型，0 表示未删除，1 表示已删除
- 在查询时过滤掉已删除的数据
- 逻辑删除可以保留数据历史，便于审计和恢复

## 7. 数据安全设计

### 7.1 密码存储

- 用户密码使用加密存储，不存储明文密码
- 建议使用 BCrypt 等安全的哈希算法

### 7.2 敏感数据保护

- 对敏感数据进行适当的保护措施
- 如手机号、邮箱等个人信息

### 7.3 SQL注入防护

- 使用参数化查询，避免 SQL 注入攻击
- 建议使用 MyBatis-Plus 等 ORM 框架

## 8. 数据库初始化脚本

```sql
-- 创建数据库
CREATE DATABASE IF NOT EXISTS medicine_box DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

USE medicine_box;

-- 用户表
CREATE TABLE IF NOT EXISTS users (
    id VARCHAR(36) PRIMARY KEY,
    username VARCHAR(50) NOT NULL,
    password VARCHAR(100) NOT NULL,
    phone VARCHAR(20) NOT NULL UNIQUE,
    email VARCHAR(100) UNIQUE,
    nickname VARCHAR(50),
    avatar VARCHAR(255),
    gender TINYINT,
    birthdate DATE,
    role TINYINT NOT NULL DEFAULT 0,
    status TINYINT NOT NULL DEFAULT 1,
    last_login_time DATETIME,
    create_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    deleted TINYINT NOT NULL DEFAULT 0
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 药品分类表
CREATE TABLE IF NOT EXISTS medicine_categories (
    id VARCHAR(36) PRIMARY KEY,
    category_name VARCHAR(50) NOT NULL,
    parent_id VARCHAR(36),
    description VARCHAR(255),
    create_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    deleted TINYINT NOT NULL DEFAULT 0,
    FOREIGN KEY (parent_id) REFERENCES medicine_categories(id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 药品表
CREATE TABLE IF NOT EXISTS medicines (
    id VARCHAR(36) PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    category_id VARCHAR(36),
    brand VARCHAR(100),
    specification VARCHAR(100) NOT NULL,
    dosage_form VARCHAR(50),
    usage TEXT,
    indications TEXT,
    contraindications TEXT,
    side_effects TEXT,
    storage_condition VARCHAR(255),
    expiry_date_days INT,
    manufacturer VARCHAR(100),
    approval_number VARCHAR(50),
    price DECIMAL(10,2),
    barcode VARCHAR(50) UNIQUE,
    status TINYINT NOT NULL DEFAULT 1,
    create_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    deleted TINYINT NOT NULL DEFAULT 0,
    FOREIGN KEY (category_id) REFERENCES medicine_categories(id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 库存表
CREATE TABLE IF NOT EXISTS inventory (
    id VARCHAR(36) PRIMARY KEY,
    user_id VARCHAR(36) NOT NULL,
    medicine_id VARCHAR(36) NOT NULL,
    batch_number VARCHAR(50),
    purchase_date DATE,
    expiry_date DATE NOT NULL,
    current_quantity INT NOT NULL,
    min_stock INT NOT NULL DEFAULT 1,
    unit VARCHAR(20) NOT NULL,
    location VARCHAR(100),
    last_update_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    deleted TINYINT NOT NULL DEFAULT 0,
    FOREIGN KEY (user_id) REFERENCES users(id),
    FOREIGN KEY (medicine_id) REFERENCES medicines(id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 库存变动记录表
CREATE TABLE IF NOT EXISTS inventory_history (
    id VARCHAR(36) PRIMARY KEY,
    user_id VARCHAR(36) NOT NULL,
    inventory_id VARCHAR(36) NOT NULL,
    medicine_id VARCHAR(36) NOT NULL,
    change_type TINYINT NOT NULL,
    change_quantity INT NOT NULL,
    before_quantity INT NOT NULL,
    after_quantity INT NOT NULL,
    reason VARCHAR(255),
    change_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(id),
    FOREIGN KEY (inventory_id) REFERENCES inventory(id),
    FOREIGN KEY (medicine_id) REFERENCES medicines(id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 用药记录表
CREATE TABLE IF NOT EXISTS usage_records (
    id VARCHAR(36) PRIMARY KEY,
    user_id VARCHAR(36) NOT NULL,
    medicine_id VARCHAR(36) NOT NULL,
    usage_quantity INT NOT NULL,
    usage_time DATETIME NOT NULL,
    notes VARCHAR(255),
    create_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(id),
    FOREIGN KEY (medicine_id) REFERENCES medicines(id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 报警表
CREATE TABLE IF NOT EXISTS alerts (
    id VARCHAR(36) PRIMARY KEY,
    user_id VARCHAR(36) NOT NULL,
    medicine_id VARCHAR(36),
    alert_type TINYINT NOT NULL,
    alert_level TINYINT NOT NULL,
    message VARCHAR(255) NOT NULL,
    status TINYINT NOT NULL DEFAULT 0,
    send_time DATETIME,
    resolve_time DATETIME,
    create_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(id),
    FOREIGN KEY (medicine_id) REFERENCES medicines(id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 报警设置表
CREATE TABLE IF NOT EXISTS alert_settings (
    id VARCHAR(36) PRIMARY KEY,
    user_id VARCHAR(36) NOT NULL,
    alert_type TINYINT NOT NULL,
    alert_level TINYINT NOT NULL,
    threshold INT,
    enabled TINYINT NOT NULL DEFAULT 1,
    notification_methods VARCHAR(50),
    update_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    deleted TINYINT NOT NULL DEFAULT 0,
    FOREIGN KEY (user_id) REFERENCES users(id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 药品推荐表
CREATE TABLE IF NOT EXISTS medicine_recommendations (
    id VARCHAR(36) PRIMARY KEY,
    category VARCHAR(50) NOT NULL,
    target_group VARCHAR(50),
    season VARCHAR(20),
    recommended_medicines TEXT,
    description VARCHAR(255),
    create_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    deleted TINYINT NOT NULL DEFAULT 0
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 通知记录表
CREATE TABLE IF NOT EXISTS notifications (
    id VARCHAR(36) PRIMARY KEY,
    user_id VARCHAR(36) NOT NULL,
    type TINYINT NOT NULL,
    title VARCHAR(100) NOT NULL,
    content TEXT NOT NULL,
    is_read TINYINT NOT NULL DEFAULT 0,
    send_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    read_time DATETIME,
    FOREIGN KEY (user_id) REFERENCES users(id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 创建索引
CREATE INDEX idx_users_phone ON users(phone);
CREATE INDEX idx_users_email ON users(email);
CREATE INDEX idx_medicines_category_id ON medicines(category_id);
CREATE INDEX idx_medicines_barcode ON medicines(barcode);
CREATE INDEX idx_inventory_user_id ON inventory(user_id);
CREATE INDEX idx_inventory_medicine_id ON inventory(medicine_id);
CREATE INDEX idx_inventory_expiry_date ON inventory(expiry_date);
CREATE INDEX idx_inventory_history_inventory_id ON inventory_history(inventory_id);
CREATE INDEX idx_usage_records_user_id ON usage_records(user_id);
CREATE INDEX idx_usage_records_medicine_id ON usage_records(medicine_id);
CREATE INDEX idx_alerts_user_id ON alerts(user_id);
CREATE INDEX idx_alerts_medicine_id ON alerts(medicine_id);
CREATE INDEX idx_alerts_status ON alerts(status);
CREATE INDEX idx_alert_settings_user_id ON alert_settings(user_id);
CREATE INDEX idx_notifications_user_id ON notifications(user_id);
CREATE INDEX idx_notifications_is_read ON notifications(is_read);
```

## 9. 数据迁移策略

### 9.1 初始数据

- 药品分类的初始数据
- 系统默认报警设置
- 常用药品推荐

### 9.2 数据备份

- 定期备份数据库
- 建议使用 MySQL 备份工具或云服务的备份功能

### 9.3 数据恢复

- 建立完善的数据恢复机制
- 测试恢复流程，确保数据安全

## 10. 性能优化策略

### 10.1 索引优化

- 根据查询频率和模式优化索引
- 避免过度索引，影响写入性能

### 10.2 查询优化

- 使用合适的查询语句
- 避免全表扫描
- 合理使用 JOIN 操作

### 10.3 存储优化

- 定期清理过期数据
- 优化表结构，减少冗余字段
- 考虑使用分区表处理大量数据

### 10.4 连接池配置

- 合理配置数据库连接池
- 避免连接泄露

## 11. 总结

本数据库设计文档详细描述了家庭药箱小助手的数据库结构、关系和设计理念。采用了 UUID 作为主键，实现了逻辑删除功能，建立了合理的索引和外键关系，确保了数据的一致性、完整性和可扩展性。

数据库设计遵循了规范化原则，同时考虑了性能优化和数据安全，为系统的稳定运行提供了坚实的基础。通过定期维护和优化，可以确保数据库在高并发场景下的良好性能。
